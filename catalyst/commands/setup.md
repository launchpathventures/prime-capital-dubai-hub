description: Initialize a new Catalyst project
description: Catalyst: Set up project
Initialize a fresh Catalyst project or add Catalyst to an existing codebase.

@catalyst/prompts/repo-setup.md

## Preflight Checks

1. Confirm you're in the project root (package.json exists)
2. Ensure dependencies are installed
3. Ensure .env.local exists (copy from .env.example if needed)
4. Optionally start the dev server

## Repo Context

Read before proceeding:
- `@AGENTS.md` — Agent instructions and repo conventions
- `@catalyst/CATALYST.md` — Framework overview
- `@catalyst/BRIEFS.md` — Brief workflow and states
