# CATALYST - AI Agent Instructions

Follow @AGENTS.md for development.

## Workspace Setup
- At session start, copy env: `cp $CONDUCTOR_ROOT_PATH/.env.local .env.local`
- Never modify `.env.local` — treat as read-only
- To change env vars, update the source file in the root project directory
